{% extends 'base.html.twig' %}

{% block title %}Formation index{% endblock %}

{% block body %}
    <div>
    <br>
    <br>
        <h1 align='center'>Liste des demandes de Formation </h1>
    <br>
        <table class="table">
            <thead>
                <tr>
                    <th>Matricule</th>
                    <th>Prenom</th>
                    <th>Nom</th>
                    <th>Telephone</th>
                    <th>Email</th>
                    <th>Formation</th>
                    <th>Ministere</th>
                    <th>Détails</th>
                </tr>
            </thead>
            <tbody>
            {% for formation in formations %}
                <tr>
                    <td>{{ formation.matricule }}</td>
                    <td>{{ formation.prenom }}</td>
                    <td>{{ formation.nom }}</td>
                    <td>{{ formation.telephone }}</td>
                    <td>{{ formation.email }}</td>
                    <td>{{ formation.formation }}</td> 
                    <td>{{ formation.ministere.nom}}</td> 
                    <td> 
                        <a href="{{ path('formation_edit', {'id': formation.id}) }}">Visualiser</a>
                        {# <a href="{{ path('formation_show', {'id': formation.id}) }}">Visualiser</a> #}
                    </td>
                </tr>
            {% else %}
                <tr>
                    <td colspan="8">no records found</td>
                </tr>
            {% endfor %}
            </tbody>
        </table>

        <a href="{{ path('formation_new') }}">Create une nouvelle demande de formation</a>
    </div>
{% endblock %}
