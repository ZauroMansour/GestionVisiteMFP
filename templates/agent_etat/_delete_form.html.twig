<form method="post" action="{{ path('agent_etat_delete', {'id': agent_etat.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ agent_etat.id) }}">
    <button class="btn">Delete</button>
</form>
